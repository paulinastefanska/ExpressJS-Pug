doctype html
html
    head
        title="Login"
    body
        h2 Log In:
        p Login
            input(type="text", name="login")
        p Password
            input(type="password", name="password")
        a.confirm(href="/auth/google") Enter